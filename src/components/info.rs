#![allow(non_snake_case)]
// use crate::services::notification::{auto_pop, push, NotificationLevels, NotificationMsg};
use chrono::Utc;
use dioxus::prelude::*;
pub(crate) fn Info(cx: Scope) -> Element {
    
    // use_future(cx, (), |_| async move {
    //     let _= auto_pop().await;
    // });
    // let _r = push(NotificationMsg {
    //     level: NotificationLevels::Error,
    //     msg: "error occured".to_string(),
    //     create_at: Utc::now(),
    // });
   
    render!(
        
    )
}
